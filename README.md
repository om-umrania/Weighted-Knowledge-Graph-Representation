# Weighted-Knowledge-Graph-Representation
This repository contains an implementation of a Knowledge Graph with Weighted Edges, where the edge thickness represents the frequency of relationships between entities. The project is inspired by Google’s Knowledge Graph (Google Blog), which shifted search from keyword-based retrieval to entity-based structured knowledge.
